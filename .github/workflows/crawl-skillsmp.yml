name: "SkillsMP Crawl"

on:
  schedule:
    - cron: "0 6 * * 6"  # Saturday 6am UTC
  workflow_dispatch: {}

jobs:
  crawl:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install requests pyyaml

      - name: Run SkillsMP crawler
        run: python scripts/crawlers/skillsmp/crawler.py

      - name: Check for proposals
        id: check
        run: |
          if [ -d proposals ] && [ "$(ls proposals/skillsmp-*.json 2>/dev/null)" ]; then
            echo "has_proposals=true" >> $GITHUB_OUTPUT
          else
            echo "has_proposals=false" >> $GITHUB_OUTPUT
          fi

      - name: Create PR
        if: steps.check.outputs.has_proposals == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          DATE=$(date +%Y-%m-%d)
          BRANCH="bot/skillsmp/$DATE"
          git checkout -b "$BRANCH"
          git add proposals/
          git commit -m "[bot] Add skills from SkillsMP ($DATE)"
          git push origin "$BRANCH"
          gh pr create \
            --title "[bot] Add skills from SkillsMP ($DATE)" \
            --body "Auto-generated by SkillsMP crawler. Review proposals/ for candidates with Class C evidence (community skills)." \
            --label "bot-proposal" \
            --label "source:skillsmp"
