{
  "name": "@gaia-registry/cli",
  "version": "0.1.0",
  "description": "Gaia CLI - Integrate your AI agent skills with the Gaia registry",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "gaia": "dist/bin/gaia.js"
  },
  "files": [
    "dist",
    "cli/**/*.py",
    "python/gaia_cli/**/*.py",
    "python/gaia_cli/**/*.json",
    "python/gaia_cli/**/*.md",
    "README.md"
  ],
  "scripts": {
    "build": "node ./scripts/sync-python-package.mjs && tsc",
    "test": "npm run build && node --test test/*.test.mjs",
    "prepare": "npm run build",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "gaia",
    "skills",
    "ai",
    "agents",
    "registry",
    "cli"
  ],
  "author": "Gaia Contributors",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "optionalDependencies": {
    "python-shell": "^5.0.0"
  }
}
